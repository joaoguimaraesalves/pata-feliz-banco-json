{
  "name": "pata-feliz-banco-json",
  "version": "1.0.0",
  "description": "API mockada (JSON Server) do sistema Pata Feliz - Banho & Tosa",
  "private": true,
  "scripts": {
    "start": "json-server db.json --host 0.0.0.0 --port ${PORT:-3000}",
    "dev": "json-server --watch db.json --port 3000"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  },
  "engines": {
    "node": ">=18"
  }
}
